import { withDesign } from 'storybook-addon-designs';
import { Meta, Story, Canvas } from '@storybook/addon-docs';
import SaveAndCloseButtonGroup from '../../../components/SaveAndCloseButtonGroup';
import Template from './Template';

export const designParams = {
  design: {
    type: 'figma',
    url: 'https://www.figma.com/file/XeXHoQxESkwlENWxfRY3Jq/Fuse-Design-System%3A-Integrator.io?node-id=6005%3A72',
  },
};

<Meta 
  title="Components/ Buttons/ SaveAndCloseButtonGroup" 
  component={SaveAndCloseButtonGroup}
  decorators={[ withDesign ]}
  parameters={designParams}
/>

# Save, Save and close, Close

If no changes have been made, then the Save button should be disabled, 
and the Close button should be active

<Canvas>
  <Story name="No unsaved changes" >
    {Template.bind({})}
  </Story>
</Canvas>

If there are unsaved changes, then all buttons and links should be active.

<Canvas>
  <Story name="Unsaved changes" args={{isDirty: true}} >
    {Template.bind({})}
  </Story>
</Canvas>

If changes are being saved, then the Saving... and Close buttons should be disabled

<Canvas>
  <Story name="Save in progress" args={{status: 'inProgress'}} >
    {Template.bind({})}
  </Story>
</Canvas>