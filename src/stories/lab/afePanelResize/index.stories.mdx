import { Meta, Story, Canvas } from '@storybook/addon-docs';
import ResizeProto from './Prototype';
import Template from './Template';

<Meta 
  title="Lab/AFE Panel Resize"
  component={ResizeProto}
  parameters={{ previewTabs: { 'storybook/docs/panel': { hidden: true } } }}
/>

<Story 
  name="Complex" 
  args={
    { panels: ['rule', 'data', 'console', 'defaults', 'help'], 
      layout: { 
      gridTemplateColumns: '1fr auto 1fr auto 1fr',
      gridTemplateRows: `2fr auto 2fr auto 150px`,
      gridTemplateAreas: `
       'rule dragBar_v_0 defaults dragBar_v_1 data' 
       'rule dragBar_v_0 dragBar_h_0 dragBar_v_1 data'
       'rule dragBar_v_0 help dragBar_v_1 data'
       'dragBar_h_1 dragBar_h_1 dragBar_h_1 dragBar_h_1 dragBar_h_1'
       'console console console console console'`,
      }
    }
  }>
  {Template.bind({})}
</Story>

<Story
  name="Compact" 
  args={
    { panels: ['rule', 'data', 'result', 'error'], 
      layout: { 
        gridTemplateColumns: '1fr auto 1fr',
        gridTemplateRows: `2fr auto 2fr auto 1fr`,
        gridTemplateAreas: `
          'rule dragBar_v_0 data' 
          'rule dragBar_v_0 dragBar_h_0'
          'rule dragBar_v_0 result'
          'dragBar_h_1 dragBar_h_1 dragBar_h_1'
          'error error error'`,
      }
    }
  }>
  {Template.bind({})}
</Story>

<Story
  name="Compact2" 
  args={
    { panels: ['rule', 'data', 'result', 'error'], 
      layout: { 
        gridTemplateColumns: '1fr auto 1fr',
        gridTemplateRows: '1fr auto 1fr auto 0fr',
        gridTemplateAreas: `
         'rule dragBar_v_0 data' 
         'dragBar_h_0 dragBar_v_0 data'
         'result dragBar_v_0 data' 
         'dragBar_h_1 dragBar_h_1 dragBar_h_1'
         'error error error'`,
      }
    }
  }>
  {Template.bind({})}
</Story>

<Story
  name="Row" 
  args={
    { panels: ['rule', 'data', 'result', 'error'], 
      layout: { 
        gridTemplateColumns: '1fr',
        gridTemplateRows: '2fr auto 2fr auto 1fr auto 150px',
        gridTemplateAreas: `
          'rule' 
          'dragBar_h_0' 
          'data' 
          'dragBar_h_1' 
          'result' 
          'dragBar_h_2' 
          'error'`,
      }
    }
  }>
  {Template.bind({})}
</Story>

<Story
  name="Compact Row" 
  args={
    { panels: ['rule', 'data', 'result', 'error'], 
      layout: { 
      gridTemplateColumns: '1fr auto 1fr',
      gridTemplateRows: '1fr auto 1fr auto 150px',
      gridTemplateAreas: `
        'rule rule rule'
        'dragBar_h_0 dragBar_h_0 dragBar_h_0'
        'data dragBar_v_0 result'
        'dragBar_h_1 dragBar_h_1 dragBar_h_1'
        'error error error'`,
      }
    }
  }>
  {Template.bind({})}
</Story>

<Story
  name="Column" 
  args={
    { panels: ['rule', 'data', 'result', 'error'], 
      layout: { 
      gridTemplateColumns: '1fr auto 1fr auto 1fr',
      gridTemplateRows: '4fr auto 150px',
      gridTemplateAreas: `
        'rule dragBar_v_0 data dragBar_v_1 result'
        'dragBar_h_0 dragBar_h_0 dragBar_h_0 dragBar_h_0 dragBar_h_0'
        'error error error error error'`,
      }
    }
  }>
  {Template.bind({})}
</Story>
