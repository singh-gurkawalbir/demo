import React from 'react';
import { Typography } from '@material-ui/core';

export default function StepperProto() {
  return (
    <Typography>stepper proto - Waiting on final from UX before proto is created.</Typography>
  );
}
