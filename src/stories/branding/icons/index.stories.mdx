import { withDesign } from 'storybook-addon-designs';
import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { IconButton, Button } from '@material-ui/core';
import AdminIcon from '../../../components/icons/AdminIcon';

<Meta title="Branding/Icons/Examples" />

# Icon Use

An Icon with no behaviors

<Canvas>
  <Story name="Simple Icon">
    <AdminIcon />
  </Story>
</Canvas>

An Icon used as a button

<Canvas>
  <Story name="Icon Button">
    <IconButton>
      <AdminIcon />
    </IconButton>
  </Story>
</Canvas>

An Icon button with Text

<Canvas>
  <Story name="Icon Text Button">
    <Button startIcon={<AdminIcon/>}>
      Icon button
    </Button>
  </Story>
</Canvas>
