import colors from './colors';

export default {
  palette: {
    background: {
      // These values are just to test the sandbox/prod theme toggle
      // We need to get the sandbox design from UX team (or make up something temporary)
      // the design objective is to have the left drawer background change to sand color.
      // TODO: Azhar, please talk to me before doing any sandbox styling so we can agree
      // on proper direction.
      paper: colors.celigoAccent4,
      default: colors.celigoAccent3,
    },
  },
};
