These are our Celigo brand colors. Both light and dark theme use the same colors, just applied uniquely for each theme.

```js
const ColorPalette = require('../src/styleguide/ColorPalette.jsx').default;

<ColorPalette />
```